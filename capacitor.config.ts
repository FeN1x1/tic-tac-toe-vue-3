import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tictactoe.vue',
  appName: 'tic-tac-toe',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
